{
  "name": "wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sozlanadigan onlayn to'y taklifnomasi — har bir mijoz uchun alohida Vercel deploy",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-config.js",
    "test": "node --test tests/*.test.js",
    "new": "node scripts/new-client.js",
    "build:all": "node scripts/build-all.js",
    "server": "node server/index.js"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "redis": "^5.12.1"
  }
}
